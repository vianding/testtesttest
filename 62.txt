1. Confirm run_pricing() and standalone LP pricing are byte-for-byte / number-for-number unchanged versus the previous committed V1/Phase 2 baseline.

2. Confirm all new V2 behaviour is opt-in through run_joint_pricing() or joint CLI only.

3. Confirm correlation is applied only to innovations/shocks, never by post-processing simulated hazard/LP/macro levels.

4. Confirm rho=0 joint mode is statistically equivalent to independent marginal simulations, even if not path-identical due to central RNG ordering.

5. Confirm LPOptionCost(external_lp_paths=None) preserves standalone Phase 2 behaviour, and external_lp_paths only affects joint mode.

6. Confirm the stochastic macro path can be disabled or made deterministic for baseline comparison.

7. Confirm the reported joint-price result around ~410 bps is explainable by inclusion of LP cost and joint dynamics, not accidental double-counting of liquidity cost.